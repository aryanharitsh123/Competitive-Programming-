#include<stdio.h>
#include<string.h>
int main() {
  char s[1001];
  scanf("%[^\n]%*c", s);
  int length,cnt=0,i,x=0;
  length = strlen(s);
  
  return 0;
}

// removed long long int , question is based on Strings only
